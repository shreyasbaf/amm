export interface DefaultTheme{
    theme: string;
    white: string;
    primaryColor: string;
    red: string;
}
const theme: DefaultTheme = {
  theme: 'dark',
  white: '#ffffff',
  primaryColor: '#10363d',
  red: '#AA4A44',
}

export { theme }