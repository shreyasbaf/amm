export interface DefaultTheme{
    theme: string;
    white: string;
    primaryColor: string;
}
const theme: DefaultTheme = {
  theme: 'dark',
  white: '#ffffff',
  primaryColor: '#10363d',
}

export { theme }